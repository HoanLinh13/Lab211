package com.mycompany.j1.s.p0074.model;

public interface IMatrix {
    void insertMatrix();
    IMatrix additionMatrix(IMatrix matrix);
    IMatrix subtractionMatrix(IMatrix matrix);
    IMatrix multiplicationMatrix(IMatrix matrix);
}